// ===================================================================
// 데이터 소스 설정
// 이 앱은 별도의 서버 없이 브라우저에서 구글 스프레드시트의 공개 조회
// API(gviz)를 직접 호출해서 데이터를 가져옵니다.
// 스프레드시트는 반드시 "공유 > 일반 액세스 > 링크가 있는 모든 사용자(뷰어)"
// 로 설정되어 있어야 합니다.
// ===================================================================

// 새로 연결된 스프레드시트 (원본 사이트는 연도별로 2개의 스프레드시트를 썼지만,
// 이 스프레드시트는 하나에 당해 연도(1~12월) 데이터가 모두 들어있는 구조입니다.)
export const SPREADSHEET_ID = "1wEoJBPiXud2Z63MQUHok-bkL7ONNIulD";

// 실제 브랜드별 시트 탭 이름 (원본은 "전포차"였으나 이 스프레드시트는 "생전포차")
export const BRAND_SHEETS = ["PC", "벌툰", "꼬치", "생전포차"];

// 전체 품목 통합 시트 (판매수량/매출액만 존재, 마진 컬럼 없음)
export const COMBINED_SHEET = "전범용품목리스트";

// 브랜드 강조색 (없는 브랜드는 기본색 사용)
export const BRAND_COLORS = {
  PC: "#3b82f6",
  벌툰: "#f59e0b",
  꼬치: "#18181b",
  생전포차: "#f97316",
};

// 화면에 표시할 연도 라벨 (매년 갱신 필요 — 스프레드시트가 그 해의 1~12월 데이터를 담고 있다는 전제)
export const CURRENT_YEAR_LABEL = "2026";

export const MONTH_LABELS = [
  "1월", "2월", "3월", "4월", "5월", "6월",
  "7월", "8월", "9월", "10월", "11월", "12월",
];
